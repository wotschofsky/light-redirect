[package]
name = "light-redirect"
version = "1.0.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "net", "macros", "signal"] }
axum = "0.8"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
